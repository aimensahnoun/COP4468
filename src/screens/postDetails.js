import {View} from 'react-native';

import CustomText from "../components/CustomText"

const PostDetails = ({navigation}) => {
    return <View>
        <CustomText>Post Details</CustomText>
    </View>
}

export default PostDetails